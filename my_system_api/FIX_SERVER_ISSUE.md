# ===================================
# دليل إصلاح مشكلة السيرفر
# ===================================

## المشكلة:
السيرفر القديم يعمل على المنفذ 53365 ويُرجع "error code: 1033" بدلاً من JSON صحيح.

## الحل السريع:

### الطريقة 1: Task Manager (الأسهل)
1. اضغط `Ctrl + Shift + Esc` لفتح Task Manager
2. اذهب إلى تبويب "Details"
3. ابحث عن عملية اسمها: **dart.exe**
4. اضغط عليها بالزر الأيمن واختر "End task"
5. أعد تشغيل السيرفر

### الطريقة 2: PowerShell (متقدم)
افتح PowerShell **كمدير (Run as Administrator)** ونفذ:

```powershell
# إيقاف جميع عمليات dart
Get-Process dart -ErrorAction SilentlyContinue | Stop-Process -Force

# أو استخدم PID المحدد
Stop-Process -Id 22712 -Force
```

### الطريقة 3: netstat + taskkill (بديل)
```powershell
# البحث عن العملية
netstat -ano | findstr "53365"

# سيظهر رقم PID في النهاية، مثل: 22712
# استخدمه هنا:
taskkill /F /PID 22712
```

## بعد إيقاف السيرفر القديم:

### 1. تشغيل السيرفر الجديد:
```powershell
cd C:\code\my_system\my_system_api\bin
dart run server.dart
```

### 2. يجب أن ترى:
```
Admin user already exists
📝 عمود archived موجود بالفعل
✅ تم إنشاء جداول الأقسام المخصصة
🚀 API يعمل على المنفذ 53365
✅ جاهز للاتصال عبر: admin.taif.digital
```

### 3. إذا رأيت خطأ المنفذ مشغول:
ارجع للطريقة 1 (Task Manager) وتأكد من إيقاف dart.exe تماماً.

## اختبار السيرفر:

بعد تشغيل السيرفر الجديد:

1. **أعد تشغيل التطبيق** (أو hot reload: اضغط `r`)
2. اذهب إلى: **تخصيص الأقسام**
3. أنشئ قسم جديد
4. راقب Console - يجب أن ترى:

```
🔄 محاولة إنشاء قسم: اسم القسم
📥 طلب إنشاء قسم جديد         <-- من السيرفر
✅ المستخدم: X
✅ تم إنشاء القسم بنجاح - ID: X
✅ استجابة API: {success: true, ...}
```

## إذا استمرت المشكلة:

### تحقق من السيرفر:
```powershell
# تأكد من عدم وجود عمليات dart
Get-Process dart -ErrorAction SilentlyContinue

# تأكد من أن المنفذ فارغ
netstat -ano | findstr "53365"
```

يجب ألا ترى أي نتائج!

### تحقق من قاعدة البيانات:
```powershell
cd C:\code\my_system\my_system_api\bin
sqlite3 database.db

# في sqlite:
SELECT * FROM custom_categories;
SELECT * FROM custom_categories_settings;
.exit
```

## ملاحظات مهمة:

1. ✅ تأكد من إيقاف السيرفر القديم **تماماً** قبل تشغيل الجديد
2. ✅ استخدم Task Manager إذا فشلت الطرق الأخرى
3. ✅ أعد تشغيل التطبيق (Flutter) بعد تشغيل السيرفر الجديد
4. ✅ راقب console logs لفهم ما يحدث

## الخطوة التالية:

بعد تشغيل السيرفر الصحيح، جرب:
1. إنشاء قسم مخصص
2. تفعيل المشاركة
3. إضافة منتجات للقسم
4. التحقق من ظهوره في الصفحة الرئيسية

---

**نصيحة:** احتفظ بـ Task Manager مفتوحاً أثناء التطوير لمراقبة عمليات dart بسهولة!
