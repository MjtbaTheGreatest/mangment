# سجل التغييرات - نظام رأس المال v2.0

## [2.0.0] - 2025-11-19

### ✨ ميزات جديدة

#### 🔧 إصلاح خصم التكلفة للموظفين
- الآن يتم خصم تكلفة الطلب من رأس المال تلقائياً عند تسجيل أي طلب من قبل **جميع المستخدمين** (المدير والموظفين)
- **قبل التحديث**: كان المدير فقط يمكنه خصم التكلفة
- **بعد التحديث**: أي موظف يسجل طلب، يتم الخصم تلقائياً

#### 📋 نافذة تفاصيل العملية المتطورة
- عند الضغط على أي عملية في سجل العمليات، تظهر نافذة منبثقة احترافية تعرض:
  * **معلومات عامة**: الوصف، المستخدم، التوقيت الدقيق
  * **تفاصيل الطلب** (إذا كانت العملية لطلب):
    - رقم الطلب
    - اسم المنتج
    - اسم الزبون
    - رقم الهاتف
    - سعر البيع
    - الربح المحقق
- تصميم جذاب مع ألوان تميز الإضافات (أخضر) والسحوبات (أحمر)

#### 🗑️ حذف معاملة واحدة
- إمكانية حذف أي معاملة فردية (للمدير فقط)
- زر حذف سريع بجانب كل عملية
- زر حذف في نافذة التفاصيل
- نافذة تأكيد قبل الحذف مع تحذير واضح

### 🔄 تحسينات

#### واجهة سجل العمليات اليومي
- تصميم محسّن لعرض العمليات
- كل عملية قابلة للنقر
- حدود ملونة حسب نوع العملية
- عرض الوصف المختصر

#### قاعدة البيانات
- إضافة 5 أعمدة جديدة لجدول `capital_transactions`:
  * `order_id` - رقم الطلب المرتبط
  * `product_name` - اسم المنتج
  * `customer_name` - اسم الزبون
  * `customer_phone` - رقم الهاتف
  * `sell_price` - سعر البيع

### 🔌 APIs جديدة

1. **خصم تكلفة طلب** (متاح للجميع)
   ```
   POST /api/capital/withdraw-order
   ```

2. **جلب تفاصيل معاملة**
   ```
   GET /api/capital/transaction/<id>
   ```

3. **حذف معاملة واحدة** (للمدير فقط)
   ```
   DELETE /api/capital/transactions/single/<id>
   ```

### 📁 الملفات المعدلة

#### Backend
- `my_system_api/bin/server.dart`
  * إضافة 3 دوال جديدة
  * تحديث نظام الصلاحيات
  * إضافة دعم تخزين تفاصيل الطلب

#### Frontend
- `lib/services/api_service.dart`
  * دالة `withdrawForOrder` محدثة مع باراميترات إضافية
  * دالة `getTransactionDetails` جديدة
  * دالة `deleteTransaction` جديدة

- `lib/screens/home_screen.dart`
  * تحديث استدعاء `withdrawForOrder` لإرسال جميع التفاصيل

- `lib/screens/capital_screen.dart`
  * دالة `_showTransactionDetailsDialog` جديدة
  * دالة `_showDeleteSingleTransactionDialog` جديدة
  * دالة `_deleteSingleTransaction` جديدة
  * دالة `_buildDetailRow` لعرض التفاصيل
  * دالة `_formatDateTime` لتنسيق التاريخ والوقت
  * تحديث `_buildDailyTransactionCard` مع التصميم الجديد

### 🔒 الأمان
- جميع العمليات محمية بـ JWT token
- عمليات الحذف للمدير فقط
- التحقق من صحة البيانات قبل أي عملية
- التحقق من كفاية رأس المال قبل السحب

### ⚠️ ملاحظات هامة
- العمليات القديمة لن تحتوي على تفاصيل الطلب (طبيعي)
- فقط العمليات الجديدة بعد التحديث ستحتوي على التفاصيل الكاملة
- عمليات الحذف لا يمكن التراجع عنها

### 📚 الوثائق الجديدة
- `CAPITAL_UPDATE_SUMMARY.md` - ملخص شامل للتحديثات
- `API_TEST_GUIDE.md` - دليل اختبار APIs الجديدة
- `USER_GUIDE_CAPITAL.md` - دليل الاستخدام للمدير والموظفين
- `CHANGELOG_CAPITAL.md` - هذا الملف

### 🐛 إصلاحات
- ✅ إصلاح: الموظفون الآن يمكنهم خصم التكلفة من رأس المال
- ✅ إصلاح: تحسين عرض التفاصيل في سجل العمليات
- ✅ إصلاح: إضافة التحقق من الصلاحيات في عمليات الحذف

### 🚀 التوافق
- متوافق مع جميع الإصدارات السابقة
- لا يتطلب تعديلات على الكود الموجود
- يعمل مع قاعدة البيانات الحالية (مع إضافة الأعمدة الجديدة تلقائياً)

---

## خطوات الترقية

1. **إيقاف السيرفر**
   ```bash
   # أوقف السيرفر الحالي
   ```

2. **تحديث الكود**
   ```bash
   git pull origin main
   # أو انسخ الملفات المعدلة
   ```

3. **تشغيل السيرفر**
   ```bash
   cd my_system_api
   dart run bin/server.dart
   ```
   - الأعمدة الجديدة سيتم إضافتها تلقائياً

4. **إعادة بناء التطبيق**
   ```bash
   flutter clean
   flutter pub get
   flutter build windows --release
   ```

5. **اختبار التحديث**
   - سجل طلب جديد كموظف
   - تأكد من خصم التكلفة
   - افتح صفحة رأس المال
   - اضغط على عملية لرؤية التفاصيل
   - جرب حذف عملية (كمدير)

---

## الإصدارات القادمة 🔮

### v2.1.0 (مخطط)
- إضافة فلترة في سجل العمليات
- تصدير التقارير إلى PDF
- إحصائيات تفصيلية عن الطلبات

### v2.2.0 (مخطط)
- إشعارات عند عمليات مهمة
- رسوم بيانية تفاعلية
- مقارنة الأداء بين الموظفين

---

**تاريخ الإصدار**: 19 نوفمبر 2025  
**المطور**: MjtbaTheGreatest  
**الحالة**: ✅ جاهز للإنتاج
