# 🎨 دليل تحويل الأيقونة إلى ICO

## ✅ البرنامج يعمل الآن!

البناء نجح باستخدام أيقونة Flutter الافتراضية.

---

## 🎯 لوضع أيقونتك الخاصة 💰

### الطريقة 1️⃣: استخدام موقع التحويل (الأسهل - دقيقتين)

1. **افتح الموقع:**
   ```
   https://convertio.co/png-ico/
   ```

2. **ارفع الملف:**
   ```
   assets\icon\AppIcon~ios-marketing.png
   ```

3. **اختر الإعدادات:**
   - Format: ICO
   - Size: 256x256 (أو Multi-size)
   
4. **حمّل الناتج واستبدله:**
   ```powershell
   # احفظ الملف المحمّل في:
   windows\runner\resources\app_icon.ico
   ```

5. **أعد البناء:**
   ```powershell
   flutter clean
   flutter build windows --release
   ```

---

### الطريقة 2️⃣: استخدام ImageMagick (بعد التثبيت)

```powershell
# بعد اكتمال تثبيت ImageMagick، أعد فتح Terminal جديد:

# أغلق هذا Terminal
# افتح Terminal جديد
# شغّل:

magick convert assets\icon\AppIcon~ios-marketing.png -define icon:auto-resize=256,128,64,48,32,16 windows\runner\resources\app_icon.ico

# ثم أعد البناء:
flutter build windows --release
```

---

### الطريقة 3️⃣: استخدام برنامج IcoFX (احترافي)

1. حمّل IcoFX: https://icofx.ro/
2. افتح `assets\icon\AppIcon~ios-marketing.png`
3. Export as ICO → احفظ في `windows\runner\resources\app_icon.ico`
4. أعد البناء

---

## 🚀 التالي: إنشاء المثبت

الآن البرنامج جاهز! لإنشاء مثبت:

```powershell
# 1. حمّل Inno Setup
# https://jrsoftware.org/isdl.php

# 2. افتح ملف:
# installer.iss

# 3. اضغط F9 أو Build → Compile

# 4. المثبت سيكون في:
# Output\TaifManagement-Setup.exe
```

---

## 📦 حالياً البرنامج موجود في:

```
build\windows\x64\runner\Release\my_system.exe
```

يمكنك تشغيله مباشرة للاختبار! ✅

---

## 💡 ملاحظة

الأيقونة الحالية افتراضية من Flutter. لوضع أيقونتك 💰:
- استخدم أي طريقة من الأعلى
- التحويل يأخذ دقيقتين فقط على الموقع
- بعدها أعد البناء وجاهز!
