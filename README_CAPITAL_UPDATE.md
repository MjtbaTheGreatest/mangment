# تحديثات نظام رأس المال 💰

## نظرة عامة

تم تطبيق تحديثات شاملة على نظام رأس المال لإصلاح مشكلة خصم التكلفة للموظفين وإضافة ميزات جديدة متطورة.

## المشكلة الأصلية ❌

كان المدير فقط يمكنه خصم تكلفة الطلب من رأس المال عند تسجيل طلب جديد. عندما يسجل الموظف طلباً، لا يتم خصم التكلفة تلقائياً.

## الحل ✅

### 1. إصلاح خصم التكلفة
- إضافة endpoint جديد `/api/capital/withdraw-order` يمكن لجميع المستخدمين استخدامه
- تحديث نظام الصلاحيات للسماح للموظفين بخصم تكلفة طلباتهم
- حفظ جميع تفاصيل الطلب مع كل عملية خصم

### 2. نافذة تفاصيل العملية
عند الضغط على أي عملية، تظهر نافذة منبثقة احترافية تعرض:

**معلومات عامة**:
- 📝 الوصف الكامل
- 👤 المستخدم الذي قام بالعملية
- ⏰ التوقيت بالتفصيل (اليوم، الشهر، السنة، الساعة)

**تفاصيل الطلب** (إذا كانت العملية لطلب):
- 🏷️ رقم الطلب
- 📦 اسم المنتج
- 👥 اسم الزبون
- 📞 رقم الهاتف
- 💵 سعر البيع
- 📈 الربح المحقق

### 3. حذف معاملة واحدة
- إمكانية حذف أي معاملة فردية (للمدير فقط)
- زر حذف سريع بجانب كل عملية
- نافذة تأكيد مع تحذير واضح

## الميزات الرئيسية 🎯

| الميزة | الوصف | المستخدمون |
|--------|-------|------------|
| خصم تكلفة تلقائي | يتم خصم تكلفة كل طلب تلقائياً | الجميع |
| تفاصيل كاملة | عرض جميع تفاصيل الطلب مع العملية | الجميع |
| حذف فردي | حذف معاملة واحدة | المدير فقط |
| حذف يومي | حذف جميع معاملات يوم | المدير فقط |
| واجهة محسّنة | تصميم جديد مع ألوان واضحة | الجميع |

## التقنيات المستخدمة 🛠️

- **Backend**: Dart + Shelf + SQLite
- **Frontend**: Flutter + Material Design
- **الأمان**: JWT Authentication
- **قاعدة البيانات**: SQLite3

## الملفات المهمة 📁

### الوثائق
- `CAPITAL_UPDATE_SUMMARY.md` - ملخص شامل للتحديثات
- `API_TEST_GUIDE.md` - دليل اختبار APIs
- `USER_GUIDE_CAPITAL.md` - دليل المستخدم
- `CHANGELOG_CAPITAL.md` - سجل التغييرات
- `README_CAPITAL_UPDATE.md` - هذا الملف

### الكود
- `my_system_api/bin/server.dart` - Backend APIs
- `lib/services/api_service.dart` - Frontend API calls
- `lib/screens/capital_screen.dart` - واجهة رأس المال
- `lib/screens/home_screen.dart` - تسجيل الطلبات

## APIs الجديدة 🔌

### 1. خصم تكلفة طلب
```http
POST /api/capital/withdraw-order
Authorization: Bearer <token>
Content-Type: application/json

{
  "amount": 50000,
  "order_id": 123,
  "product_name": "نتفلكس",
  "customer_name": "أحمد",
  "customer_phone": "0770...",
  "sell_price": 60000
}
```

### 2. جلب تفاصيل معاملة
```http
GET /api/capital/transaction/123
Authorization: Bearer <token>
```

### 3. حذف معاملة (مدير فقط)
```http
DELETE /api/capital/transactions/single/123
Authorization: Bearer <token>
```

## خطوات التثبيت 🚀

### 1. تحديث Backend
```bash
cd my_system_api
# الكود الجديد موجود في bin/server.dart
dart run bin/server.dart
```

### 2. تحديث Frontend
```bash
flutter clean
flutter pub get
flutter build windows --release
```

### 3. الاختبار
```bash
# اختبر تسجيل طلب من قبل موظف
# تأكد من خصم التكلفة
# افتح صفحة رأس المال واضغط على عملية
# جرب الحذف كمدير
```

## الاختبار 🧪

راجع ملف `API_TEST_GUIDE.md` للحصول على سيناريوهات اختبار مفصلة.

## الأسئلة الشائعة ❓

### س: هل يؤثر التحديث على البيانات الموجودة؟
**ج**: لا، التحديث متوافق مع البيانات الموجودة. العمليات القديمة ستبقى كما هي، والعمليات الجديدة فقط ستحتوي على التفاصيل الكاملة.

### س: هل يحتاج إلى migration للداتابيس؟
**ج**: لا، الأعمدة الجديدة تُضاف تلقائياً عند بدء السيرفر.

### س: ماذا عن الموظفين الحاليين؟
**ج**: سيعمل كل شيء تلقائياً. لا حاجة لتغيير صلاحياتهم.

## الدعم الفني 🆘

إذا واجهت أي مشكلة:
1. راجع الـ logs في console السيرفر
2. تأكد من أن السيرفر يعمل على المنفذ الصحيح
3. تحقق من الـ token في الطلبات
4. راجع قاعدة البيانات للتأكد من إضافة الأعمدة

## المساهمة 🤝

هذا المشروع خاص بـ Taif Digital. للاستفسارات، تواصل مع الفريق التقني.

## الترخيص 📄

© 2025 Taif Digital. جميع الحقوق محفوظة.

---

**آخر تحديث**: 19 نوفمبر 2025  
**الإصدار**: 2.0.0  
**الحالة**: ✅ جاهز للإنتاج
